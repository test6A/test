# 冒烟测试模板
请基于当前仓库生成并直接执行一条全流程冒烟脚本。

任务类型：
- 全流程冒烟测试
- 生成后立即执行：是

任务目标：
- 目标链路：
- 覆盖范围：正向 / 反向 / 正反向
- 例如：提交申请 -> 审批通过 -> 流程结束
- 或：提交申请 -> 审批驳回 -> 申请人编辑/再提交 -> 审批通过 -> 流程结束

项目与环境：
- 项目名称：
- 环境：
- 入口 URL：
- 优先参考脚本：
- 优先复用 helper：

测试用例：
- 测试用例 JSON：
- 补充说明 Markdown（可选）：
- tcId（可留空自动生成）：
- sourcePointId（可留空自动生成）：

关键角色：
- 当前登录账号：
- 申请人：
- 审批人：
- 第二审批人：
- 是否允许切人：
- 是否允许真实提交：
- 是否允许真实审批：
- 是否允许真实驳回：
- 是否允许再次提交：

关键业务数据：
- 商机号：
- 单据号或项目名称：
- 客户名称：
- 产品型号：
- 其他唯一主键：

前置条件：
- 当前单据状态：
- 是否已有草稿：
- 是否已有待办：
- 是否已配置审批链：
- 是否已存在相关 CRM 报价单或关联单据：
- 其他必须前置条件：

录制脚本约束：
- 提交录制脚本：
- 审批录制脚本：
- 驳回/再提交录制脚本（如有）：
- 规则：关键动作顺序必须优先沿用录制脚本；允许优化 locator 和等待，不允许改业务动作顺序、页面跳转顺序和关键状态确认顺序

页面截图说明（可选）：
- 截图 1：
- 截图 1 用途：
- 截图 1 重点识别内容：
- 截图 2：
- 截图 2 用途：
- 截图 2 重点识别内容：
- 截图 3：
- 截图 3 用途：
- 截图 3 重点识别内容：

必须校验：
- 提交成功判定：
- 审批通过或驳回成功判定：
- 再提交成功判定：
- 最终流程结束态判定：
- 是否必须按本次业务主键回捞同一张单据：是 / 否
- 是否需要校验我的申请 / 我的待办 / 我的已办状态变化：
- 是否需要校验审批意见、驳回原因、流转状态：

禁止项：
- 不允许使用的弱判定：
- 不允许跳过的业务规则：
- 不允许误操作的页面或列表：

执行信息：
- 执行方式：单条 / grep / 单个 spec
- 最近一次失败命令：
- 最近一次失败摘要：
- 参考报告路径（可选）：

输出要求：
- 直接生成并执行，不要只给方案
- 返回脚本路径
- 返回执行结果摘要
- 若失败，按阶段说明失败点、原因、expected、actual、pageUrl

# 单页面测试模板
请基于当前仓库生成并直接执行一条单页面功能测试脚本。

任务类型：
- 单页面功能测试
- 生成后立即执行：是

任务目标：
- 页面类型：列表页 / 查询页 / 编辑页 / 弹窗 / 明细页
- 功能点：
- 例如：搜索、重置、分页、排序、字段输入、字段联动、显隐、只读、禁用、按钮状态、局部保存、提示校验

项目与环境：
- 项目名称：
- 环境：
- 页面 URL：
- 页面名称：
- 模块名称：
- 优先参考脚本：
- 优先复用 helper：

测试用例：
- 测试用例 JSON：
- 补充说明 Markdown（可选）：
- tcId（可留空自动生成）：
- sourcePointId（可留空自动生成）：

前置条件：
- 当前登录账号：
- 进入页面前需要打开的菜单或标签：
- 页面初始状态要求：
- 是否需要已有指定列表数据：
- 是否需要已有指定记录：
- 是否需要空结果场景：
- 其他必须前置条件：

关键测试数据：
- 搜索关键字：
- 查询条件组合：
- 分页大小：
- 目标记录名称：
- 字段输入值：
- 联动字段前置值：
- 非法输入样例：
- 边界值样例：
- 其他唯一定位数据：

页面截图说明（可选）：
- 截图 1：
- 截图 1 用途：
- 截图 1 重点识别内容：
- 截图 2：
- 截图 2 用途：
- 截图 2 重点识别内容：
- 截图 3：
- 截图 3 用途：
- 截图 3 重点识别内容：

必须校验：
- 搜索结果应校验哪些字段：
- 翻页后应校验哪些内容：
- 重置后应恢复哪些默认状态：
- 输入后应出现哪些联动变化：
- 哪些字段必须只读或禁用：
- 哪些按钮应可点或不可点：
- 是否需要校验必填、格式、长度、特殊字符限制：
- 是否需要校验无结果态、空列表态、分页总数、分页跳转：

执行限制：
- 是否禁止真实提交：
- 是否允许点击保存但不提交：
- 是否允许修改现有数据：
- 是否需要执行后恢复页面状态：

执行信息：
- 执行方式：单条 / grep / 单个 spec
- 最近一次失败命令：
- 最近一次失败摘要：
- 参考报告路径（可选）：

输出要求：
- 直接生成并执行，不要只给方案
- 返回脚本路径
- 返回执行结果摘要
- 若失败，按阶段说明失败点、原因、expected、actual、pageUrl